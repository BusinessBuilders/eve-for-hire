# Handoff — eve.center / Eve work (2026-07-02)

**Follow `~/eve-master-goal.md`** — that is the source of truth; all three workstreams (A redesign,
B flow, C OpenClaw) are its checklists. This handoff carries state + the open items so a fresh
session continues without re-deriving anything.

## Access / environment (facts — don't re-derive)
- Repo: `~/eve-for-hire-bus176`, branch `main`, push → GitHub Actions auto-deploys to Contabo VPS.
- VPS: `ssh nova "ssh eve_vps '<cmd>'"` (double-hop; key on nova). App at `/var/www/eve-for-hire-app`, PM2 `eve-for-hire`, served by **Caddy** (not the legacy eve-landing nginx).
- Deploy pipeline: FIXED this session (was silently failing since May on `git pull` unrelated-histories; now `fetch`+`reset --hard` + `prisma migrate deploy`). Prod Prisma was baselined.
- Local dev: `.env` in repo root (gitignored) has prod OpenClaw/Porkbun creds + a local SQLite DB; `npm run dev` → :3003. SMTP sink + OpenClaw tunnel were session-scoped (may be gone).
- **Prod Stripe is TEST mode** (sk_test/pk_test) — test card 4242… works, no real money.
- Porkbun creds valid (ping SUCCESS) but **account appears to have NO payment method** (`/domain/create` → HTTP 500). William must add a card/funds before any domain registers.
- OpenClaw on nova: gateway `openclaw-gateway` (systemd --user, port 18789), upgraded to **2026.6.11** this session; token rotated; audit in `docs/openclaw-audit-2026-07-02.md`. Backup at `/mnt/ssd/openclaw-audit-backup-20260702-114530`.

## Done this session (deployed + verified in prod unless noted)
- **B (flow):** deploy pipeline repaired; login/chat verified working; **Porkbun domain search made non-blocking** (rate-pacer, cache, live "checking…" card, DNS-wait panel). Prod verified.
- **A (redesign):** token-driven mobile-first rebuild (`DESIGN.md`, `app/page.tsx`, `app/page.module.css`), hero listening-orb SVG (`public/hero-orb.svg`), before/after in `docs/redesign-2026-07-before-after.md`. Live.
- **Agent-hosting section** added (hero leads with hosting; `public/agent-mesh.svg` = Eve→Hermes+swarm diagram). Live. Note: it's an honest **architecture** showcase, NOT a live Hermes demo.
- **C (OpenClaw):** audit + F1 (token rotate/de-hardcode), F2/F3 (proxy bound to 127.0.0.1), F4 (telegram groups disabled); upgrade to 2026.6.11 (fix: zai apiKey added inline to `models.providers.zai` in openclaw.json). Funnel re-verified after each change.
- **Art (standalone, not on site):** `art/eve-eden-poster.svg`, `art/eve-eden-emblem.svg` (+ png previews).

## OPEN — next steps (priority order)
1. **Checkout handoff bug (revenue-blocking, William's top ask).** In the real UI flow the freemium
   "10 free messages" wall triggers during qualifying, and the paywall CTA "Build your site — $89"
   links to `/#hire` (homepage) instead of creating the Stripe checkout for the pending order.
   Customer can't pay for the site+domain they configured; domain never registers.
   **Fix:** make the paywall CTA (and a real "Checkout" affordance) `POST /api/orders/checkout`
   for the in-progress order (carrying `desiredDomain`); let a ready-to-buy customer past the gate.
   Files: `app/chat/page.tsx` (paywallHit card, upgradeUrl), `app/api/chat/route.ts` (402/CHECKOUT_READY),
   `app/api/orders/checkout/route.ts`. Then re-run the UI purchase in test mode → confirm hireeve.com registers.
2. **Register hireeve.com** — available at $11.08; blocked until Porkbun has a payment method (William).
   Then either the fixed UI flow registers it, or backend `POST /domain/create {domain:'hireeve.com',years:1}`.
3. **GitHub review + safe-iteration flow** — full design proposal in
   `docs/superpowers/specs/2026-07-02-eve-github-review-flow-design.md` (repo-per-site + push-for-review PR
   + CI gate + deploy-on-merge; wires the orphaned `lib/github/rollback.ts`). Needs William's approval on the
   recommended defaults, then `writing-plans` → implement B+D first.
4. **"Open design" integration (E)** — William wants an open-source design workflow (like awesome-claude-design)
   for better generated sites. **Need the exact GitHub URL** before designing.
5. **Live Hermes demo (optional)** — jarvis-sidecar A2A adapter (`eve-a2a-hermes`) is NOT deployed on nova;
   standing it up is a security-sensitive project (opens `hermes -z --ignore-rules`). Confirm with William before doing.

## Guardrails (from goal doc)
Never break the $89 funnel; verify by running + browser, not assuming; secrets never printed/committed
(`chmod 600`); don't push to `main` until verified (push = prod deploy); no emoji in UI; can't enter card
details or add payment methods.
